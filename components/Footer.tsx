import { LogoFooter } from '@/assets/icons';

const Footer = (): JSX.Element => (
  <div className="flex items-center justify-center p-10 lg:pt-40 lg:pb-20">
    <LogoFooter />
  </div>
);

export default Footer;
